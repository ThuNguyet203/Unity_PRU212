using UnityEngine;

public class BoomSpawner : TeddySpawner
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        spawTime = 10f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
